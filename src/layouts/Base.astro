---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}
const { title, description = '每日一篇 AI 文章，講解 AI 新知、工具同應用。' } = Astro.props;
---
<!doctype html>
<html lang="zh-Hant">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <title>{title === 'AI 每日文章' ? title : `${title} · AI 每日文章`}</title>
  </head>
  <body>
    <header class="site-header">
      <div class="container">
        <a class="brand" href={import.meta.env.BASE_URL}>🤖 AI 每日文章</a>
      </div>
    </header>
    <slot />
    <footer class="site-footer">
      <div class="container">
        <p>© 2026 AI 每日文章 · 每日由 AI 自動生成</p>
      </div>
    </footer>
  </body>
</html>
